# Suivi des applications

Audit complet — accessibilité (WCAG 2.2 AA), design portail, pédagogie PER.  
Dernière mise à jour : 2026-07-07 — audit v2 surfaces-5h6h  
Campagne audit accessibilité v2 démarrée le 2026-06-16

> La colonne **« Audité v2 »** suit le nouveau cycle d'audit harmonisé **PER + accessibilité** (arbitrage croisé, distinction drill / instance). Un **❌** signifie « pas encore repassé selon la nouvelle doctrine », et **non** « non conforme ». Les correctifs déjà présents dans les fichiers restent en place.

---

## Correctifs globaux

| Fichier | Modification |
|---------|-------------|
| `apps/feedbackUtils.js` | `launchConfetti()` : vérification `prefers-reduced-motion` ajoutée (bénéficie à toutes les apps) |
| `apps/app-base.css` | `.question-box` : font-size `clamp(17→18px)`, `line-height: 1.5` ; `.feedback` : font-size `clamp(16→18px)` ; `.verify-btn` + `.new-btn` : `min-height: 64px` ; bloc `prefers-reduced-motion` ajouté |
| `apps/app-base.css` (v2) | `.back-link` : `display:inline-flex`, `align-items:center`, `min-height:44px`, `:focus-visible` ; `.level-bar gap` 10→12px ; `.level-btn` : `min-width:44px`, `:focus-visible` (inactif/actif) ; `.verify-btn` + `.new-btn` : `min-width:64px`, `:focus-visible` ; transitions déplacées dans `(prefers-reduced-motion: no-preference)` (bénéficie aux 9 apps utilisant app-base.css) |
| `apps/app-base.css` (2026-06-19) | `.level-btn`, `.verify-btn`, `.new-btn` : `font-size` porté à `22px` (seuil texte interactif, `skills/accessibilite/SKILL.md` §2) ; bénéficie à toutes les apps utilisant `app-base.css` |
| `apps/_template.html` | `role="banner"` sur header ; `role="status" aria-live="polite" aria-atomic="true"` sur feedback ; `newBtn.focus()` dans `showFeedback` |
| `apps/app-base.css` (2026-06-23) | `--app-wrong-fg` : #991B1B → #891818 (contraste 6,8:1 → 7,8:1, seuil `skills/accessibilite/SKILL.md` §2) ; bénéficie à toutes les apps utilisant app-base.css |

---

## État des apps

| Fichier | Titre | Fonctionne | Audité v2 | Notes d'audit v2 |
|---------|-------|------------|-----------|------------------|
| balance.html | La balance | ✅ | ✅ | **Accessibilité :** focus replacé sur la consigne après « Nouvel exercice » (le bouton se masquait en gardant le focus) ; accès clavier des plateaux N1/N2 (tabindex/role/aria-label + activation Entrée/Espace, le tap reste l'option principale) ; contraste `back-link` relevé (0.85→0.92, ~4,3→~5,5:1) ; `click-hint` 15→16px. **Pédagogie :** RAS — feedbacks formatifs et retry N3 conservés intacts. **Forme :** `TYPE_TACHE` déclaré (table par niveau : N1/N2 `drill`, N3 `instance`) documentant le comportement existant. **Migration app-base.css (2026-06-21) :** appliquée — `<link app-base.css>` ajouté, classes dupliquées retirées du `<style>` inline (`.level-bar`, `.level-btn`, `.action-row`, `.verify-btn`, `.new-btn`, `.question-box`, `.feedback`), `:root { --app-header-bg: #C0272D }` (branche Mathématiques) déclaré. Effet voulu : texte interactif porté à 22px (seuil `skills/accessibilite/SKILL.md`). Aucun autre changement visuel : le survol de `.verify-btn` d'app-base.css a été rendu dérivé de la couleur de branche (`filter: brightness(0.85)` au lieu d'un navy figé), ce qui redonne sur balance un rouge foncé équivalent à l'ancien `#A01F23`. Variables `--color-units/tens/hundreds` non déclarées : balance n'affiche pas de valeurs de position, palette jugée non applicable. **Migration appUtils.js (2026-06-21) :** appliquée — `<script src="appUtils.js"></script>` ajouté avant le script de l'app ; `randInt`, `shuffle`, `pick` redéfinis en inline retirés (désormais fournis par appUtils.js). Comportement inchangé vérifié en preview : génération aléatoire des exercices intacte aux trois niveaux (N1 : 12 exercices distincts sur 12 « Nouvel exercice » ; N2/N3 variés), aucune erreur console. Bénéfice secondaire : boucle de tabulation Tab/Shift+Tab d'appUtils.js désormais active. |
| capacites-1h2h.html | Qui contient le plus ? (1H-2H) | ✅ | ✅ | **Issu de la scission de `capacites.html`** (validée par Isaac : « une app = un groupe d'années »). Cible **1H-2H**. N1 = 2 récipients même famille · N2 = 3 récipients même famille → comparaison du **niveau d'eau visible**, sans piège « conservation du volume » (trop précoce à cet âge). **Accessibilité :** contraste `back-link` 0.85→0.92 (~4,3→~5,5:1) ; cibles tactiles, accès clavier des récipients, focus, feedback `role="status"`, `prefers-reduced-motion` déjà conformes. **Pédagogie :** feedbacks formatifs et rendu canvas conservés intacts. **Forme :** `TYPE_TACHE='drill'` + `showFeedback` canonique. `data-harmos="c1"` conservé sur la carte car c'est la valeur testée par le filtre cycle (le groupe d'années est porté par le badge `1H–2H`). |
| capacites-3h4h.html | Qui contient le plus ? (3H-4H) | ✅ | ✅ | **Issu de la scission de `capacites.html`.** Cible **3H-4H**. N1 = 3 récipients formes mixtes · N2 = 4 récipients, écarts resserrés (`minVisVolRatio` 0.08) → piège « **conservation du volume** » assumé (le plus haut ne contient pas toujours le plus d'eau). **Accessibilité :** contraste `back-link` 0.85→0.92 ; reste conforme (idem ci-dessus). **Pédagogie :** feedbacks formatifs et rendu canvas conservés intacts. **Forme :** `TYPE_TACHE='drill'` + `showFeedback` canonique. `data-harmos="c1"` conservé (valeur du filtre cycle ; groupe porté par le badge `3H–4H`). |
| comparaison-nombres-3h4h.html | Le comparateur de nombres (3H-4H) | ✅ | ✅ | **Accessibilité :** légende valeur-de-position passée de 15px à 18px (seuil minimum du contenu non interactif) ; contraste de la légende `#64748B` sur blanc ≈ 4,76:1 (conforme, taille seule corrigée). Cibles tactiles (symbol-btn ≥64px, texte ≥22px), focus, feedback `role="status"`, `prefers-reduced-motion` déjà conformes. **Pédagogie :** RAS — feedbacks formatifs (décomposition dizaines/unités) et coloriage valeur-de-position (objet d'apprentissage de la comparaison) conservés intacts. **Forme :** `TYPE_TACHE='drill'` déclaré et documenté — choix binaire `<`/`>`, le retry serait un tâtonnement aveugle ; comportement existant (réponse au tap, l'erreur clôt l'item) déjà conforme au contrat drill. `data-harmos="c1"` conservé sur la carte (valeur testée par le filtre cycle ; le groupe d'années est porté par le badge `3H–4H`). |
| comparaison-nombres-5h6h.html | Le comparateur de nombres (5H-6H) | ✅ | ✅ | **Audit v2 (routine) :** accessibilité (✓/✗, focus, légende 18px, header inline retiré, indication d'action additive), migration app-base.css/appUtils.js, `TYPE_TACHE='drill'`. **Décisions Isaac (2026-06-22) :** (1) N2 plafonné à 1000 (min=100, max=1000 — inclut la frontière mille, plage PER 5H-6H respectée) ; (2) fichier renommé `comparaison-nombres.html` → `comparaison-nombres-5h6h.html` + href mis à jour dans index.html ; (3) feedback enrichi avec raisonnement par valeur de position — `buildMsg` compare centaines → dizaines → unités (ou milliers si un nombre est 1000), messages corrects et erronés généralisés pour tous les rangs. Nouvelle app 7H-8H créée en parallèle. |
| comparaison-nombres-7h8h.html | Le comparateur de nombres (7H-8H) | ✅ | ✅ | Nouvelle app (7H-8H, MSN 22). Issue de la décision de scission. N1 = 1000–9999 (4 chiffres) · N2 = 10000–99999 (5 chiffres). **Audit v2 (routine) :** **Accessibilité :** RAS — cibles tactiles (symbol-btn ≥64px + texte ≥22px), légende 18px + contraste #64748B/blanc ≈4,76:1, jamais de couleur seule (✓/✗ ajoutés au libellé + feedback textuel), animations sous `prefers-reduced-motion`, focus géré (newBtn après réponse, premier symbole sur nouvel exercice), feedback `role="status"` persistant avec indication d'action, `aria-pressed`/`role="group"` conformes. **Pédagogie :** RAS — `buildMsg` formatif (raisonnement rang par rang, du plus élevé au plus bas), coloriage valeur-de-position (objet d'apprentissage de la comparaison), plages 1000–99999 dans le PER 7H-8H ; `TYPE_TACHE='drill'` cohérent (choix binaire `<`/`>`, le retry serait un tâtonnement aveugle). **Forme :** RAS — carte index.html conforme (data-domain/harmos/per, badge 7H–8H, icône, aria-label), migration app-base.css + appUtils.js déjà effectuée, structure canonique. `data-harmos="c2"` conservé (valeur testée par le filtre cycle ; le groupe d'années est porté par le badge `7H–8H`). |
| decodi-comprehension-5h6h.html | Compréhension DéCoDi (5H-6H) | ✅ | ✅ | **Scission de `decodi-comprehension.html` (décision Isaac 2026-06-24) — Modules 6–10.** Mêmes correctifs d'accessibilité que l'app originale (v2 déjà réalisée) : couleur `#7A6200`, polices 22px/18px, min-heights 64px, `:focus-visible`, `prefers-reduced-motion`, focus repositionné, indication d'action additive. **CONFLIT À ARBITRER :** `design-portail/SKILL.md` liste `#B8960C` pour le header Français — corrigé à `#7A6200` (CLAUDE.md + accessibilité l'emportent) ; le skill doit être mis à jour par Isaac. |
| decodi-comprehension-7h8h.html | Compréhension DéCoDi (7H-8H) | ✅ | ✅ | **Pédagogie :** PER L1 21 (Compréhension de l'écrit, C2 / 7H-8H) confirmé — trois types de questions (explicite, implicite, opinion avec exemple de réponse) cohérents avec les attentes fondamentales PER (inférence, lecture autonome, stratégies). `TYPE_TACHE='drill'` approprié : QCM → l'erreur clôt la question ; opinion → production libre sans bonne/mauvaise réponse. RAS sur le contenu pédagogique. **Accessibilité :** `.q-type-badge` font-size 12px → 18px (seuil contenu non interactif, skill §2) ; `.q-text` line-height 1.4 → 1.5 (interlignage minimum, skill §2) ; focus ajouté sur le bouton « J'ai lu, commencer les questions → » au démarrage de la phase lecture dans `startText()` (règle focus § 6). Autres points déjà conformes : cibles tactiles ≥64px / ≥44px, texte interactif 22px, `:focus-visible` sur tous les éléments, `prefers-reduced-motion` guards, `role="status" aria-live="polite" aria-atomic="true"` sur feedback, `role="banner"`, accordéon `aria-expanded` dynamique, focus géré sur toutes les autres transitions de vue. **Forme :** carte index.html conforme (`data-domain="francais"`, `data-harmos="c2"`, `data-per="L1 21"`, badge `.badge-7h8h`, `.launch-btn.francais`, `icon-francais`). Migration app-base.css + appUtils.js + feedbackUtils.js déjà effectuée. `--app-header-bg: #7A6200` conforme (couleur branche Français, CLAUDE.md + accessibilité). Aucun CONFLIT À ARBITRER. Aucun point À VALIDER PAR ISAAC. |
| decodi-dictee-5h6h.html | Dictée DéCoDi (5H-6H) | ✅ | ✅ | **Accessibilité :** `--app-header-bg` corrigé `#B8960C` → `#7A6200` (contraste blanc/fond 2,67:1 → 5,3:1) ; focus outline `word-input` idem ; `listen-btn` font-size 18→22px, min-height 54→64px, `:focus-visible` ajouté, hover/active recalculés ; `select-btn` font-size 17→22px, min-height 52→64px, hover border-color mis à jour, `:focus-visible` ajouté ; `nav-back-btn` font-size 15→22px, `:focus-visible` ajouté ; `recopy-input` min-height 60→64px ; textes informatifs portés à 18px (`.nav-bar-title`, `.select-section-title`, `.bloc-info`, `.recopy-label`, `.recopy-ok`, `.recap-detail`) ; `.bloc-btn-title` retire `font-size:17px` (hérite 22px) ; `.bloc-btn-words` 12→18px (voir CONFLIT) ; compteur "Mot X/Y" inline 14→18px ; `role="banner"` sur `<header>` ; `role="status" aria-live="polite" aria-atomic="true"` sur `#feedback` ; focus géré après vérification correcte, recopie validée, récapitulatif, et toutes les transitions de vue. **Pédagogie :** RAS — `TYPE_TACHE='drill'` déclaré et cohérent (une tentative par mot ; la recopie est un étayage, pas un retry). Feedbacks formatifs et données DECODI_DATA conservés intacts. **Forme :** RAS — migration app-base.css + appUtils.js + feedbackUtils.js déjà effectuée. **Décisions Isaac (2026-06-26) :** (1) `.bloc-btn-words` à 18px retenu — compromis entre seuil strict 22px et lisibilité de la hiérarchie titre/prévisualisation dans le bouton ; (2) `data-per` corrigé L1 21 → **L1 26** (Fonctionnement de la langue / orthographe lexicale) dans `index.html` + badge de la carte mis à jour. |
| decodi-dictee-7h8h.html | Dictée DéCoDi (7H-8H) | ✅ | ✅ | Anciennement `decodi-dictee-5h6h.html` — renommé 2026-06-24 (Modules 11–15 = C2 / 7H-8H, l'ancienne étiquette 5H-6H était erronée). **Accessibilité :** `--app-header-bg` corrigé `#B8960C` → `#7A6200` (contraste blanc/fond 2,78:1 → 5,78:1) ; `.listen-btn` font-size 18→22px, min-height 54→64px, hover/active recalculés (`#685300`/`#554400`), `:focus-visible` ajouté ; `.select-btn` font-size 17→22px, min-height 52→64px, hover accent mis à jour (`var(--app-header-bg)`), `:focus-visible` ajouté ; `.nav-back-btn` font-size 15→22px, `:focus-visible` ajouté ; textes informatifs portés à 18px (`.nav-bar-title`, `.select-section-title`, `.bloc-info`, `.recopy-label`, `.recopy-ok`, `.recap-detail`) ; `.bloc-btn-title` retire `font-size:17px` (hérite 22px) ; `.bloc-btn-words` 12→18px (reprend la décision Isaac du fichier 5H-6H identique) ; compteur "Mot X/Y" inline 14→18px ; `.recopy-input` min-height 60→64px ; `.word-input:focus` outline color `#B8960C` → `var(--app-header-bg)` ; transitions déplacées dans `@media (prefers-reduced-motion: no-preference)` ; `aria-describedby="recopy-word-display"` ajouté sur `recopy-input` (annonce le mot à recopier aux lecteurs d'écran) ; focus géré après réponse correcte, recopie validée, récapitulatif, et toutes les transitions de vue. **Pédagogie :** `TYPE_TACHE='drill'` déclaré et cohérent (une tentative par mot ; la recopie est un étayage, pas un retry sur l'item). DECODI_DATA, feedbacks formatifs et structure de navigation conservés intacts. **Forme :** migration app-base.css + appUtils.js + feedbackUtils.js déjà effectuée. Carte index.html conforme (`data-domain="francais"`, `data-harmos="c2"`, `data-per="L1 26"`, badge `7H–8H`). Aucun CONFLIT À ARBITRER. Aucun point À VALIDER PAR ISAAC. |
| place-le-nombre-3h4h.html | Place le nombre (3H-4H) | ✅ | ✅ | **Pédagogie :** PER MSN 12 (C1, 3H-4H) — Représentation sur droite graduée 0–100. N1 = dizaines (10..90) · N2 = nombres quelconques (unités entre dizaines). Plages et progression conformes. `TYPE_TACHE='instance'` déclaré et cohérent : le repère peut être ajusté et revérifié sur la même instance jusqu'à réussite ; le retry était déjà implémenté via `state.answered`, maintenant formalisé. Feedbacks formatifs (décomposition par dizaines) conservés intacts. **Accessibilité :** `colorForRank` redéfinie inline → retirée (fournie par appUtils.js, CLAUDE.md interdiction #2) ; `pick()` substitué à `Math.random()` direct (utilitaire fourni par appUtils.js) ; focus après réponse correcte ajouté (`newBtn.focus()`) ; focus sur `feedbackEl` (tabindex="-1") après réponse incorrecte ; focus sur `nlWrap` après « Nouvel exercice » et changement de niveau ; `tabindex` retiré du feedbackEl à la réinitialisation. Fonction `showFeedback` canonique introduite comme point unique d'appel des feedbackUtils. **Forme :** styles inline `.app-header`, `.back-link`, `.back-link:hover`, `.app-header h1`, `.header-spacer` retirés (déjà couverts par app-base.css via `--app-header-bg`) ; icône `▶` ajoutée sur `new-btn` (recommandée C1, cohérence visuelle). Aucun CONFLIT À ARBITRER. Aucun point À VALIDER PAR ISAAC. |
| place-le-nombre-5h6h.html | Place le nombre (5H-6H) | ✅ | ✅ | **Accessibilité :** `colorForRank` redéfinie inline → retirée (fournie par appUtils.js, CLAUDE.md §2) ; `Math.random()` direct → `pick()` (utilitaire fourni par appUtils.js) ; styles inline dupliquant app-base.css retirés (`.app-header`, `.back-link`, `.back-link:hover`, `.app-header h1`, `.header-spacer`, `main`) ; `feedbackEl.removeAttribute('tabindex')` à la réinitialisation ; `showFeedback(correct, msg)` introduit comme point unique d'appel des feedbackUtils ; focus géré : `newBtn.focus()` après réponse correcte, `feedbackEl.focus()` (tabindex=-1) après réponse incorrecte, `nlWrap.focus()` après « Nouvel exercice » et changement de niveau (via wrapper `newExercise()`). **Pédagogie :** `TYPE_TACHE='instance'` déclaré et cohérent — placer un repère sur la droite est corrigeable, retry déjà implémenté (via `state.answered`), maintenant formalisé. Feedbacks formatifs et droite canvas conservés intacts. **Forme :** icône `▶` ajoutée sur `new-btn` (recommandée C2, cohérence visuelle). Aucun CONFLIT À ARBITRER. Aucun point À VALIDER PAR ISAAC. |
| ville-et-campagne-5h6h.html | Ville et campagne (5H-6H) | ✅ | ✅ | **Pédagogie :** `TYPE_TACHE = { sort: 'instance', qcm: 'drill', gap: 'instance' }` déclaré. Exercice 1 (classement) et exercice 3 (gap fill) classés instance signifiante : le feedback formatif liste les erreurs → retry sur la même instance jusqu'à réussite totale ; le bouton « Voir mon résultat » n'apparaît qu'après succès complet. Exercice 2 (QCM) classé drill : une réponse par question, l'erreur clôt l'item. Contenu pédagogique (données, feedbacks formatifs) conservé intact. **Accessibilité :** Tailles de texte interactif portées à 22px (sort-item, answer-btn, gap-option, next-btn, home-btn, exercise-card-title — étaient 14–18px). Textes non interactifs portés à 18px minimum (sort-question clamp(17→18px), question-text clamp(17→18px), gap-sentence-text clamp(15→18px), exercise-card-desc 14→18px, exercise-card-badge 13→18px, home-desc 15→18px, mini-result-msg clamp(15→18px), drop-zone-label 14→18px, progress-label 13→18px, pool-label 13→18px, score-badge 14→18px). Cibles tactiles portées à 64px (sort-item 48→64, answer-btn 52→64, gap-option 44→64, next-btn 52→64, home-btn 56→64). Contrastes insuffisants corrigés : pool-label et progress-label #94A3B8→#475569 (~2,3:1→~6,5:1) ; home-desc et mini-result-msg #64748B→#475569 ; answer-btn.wrong color #991B1B→var(--app-wrong-fg) (#891818, 7,8:1). Redéfinition inline de `.feedback` retirée (font-size 15px), `.feedback.correct`/`.wrong` retirées — utilise app-base.css (18px, conforme) ; seule `.feedback.warning` conservée inline. `aria-atomic="true"` ajouté sur fb-sort, fb-qcm, fb-gap. `aria-pressed` ajouté sur les .gap-option (false/true dynamique). Focus visible ajouté sur sort-item, answer-btn, gap-option, exercise-card, next-btn, home-btn, drop-zone. Gestion de focus complète : startSort/startQcm/startGap focalisent le premier élément interactif ; answerQcm focalise next-qcm-btn ; vérification sort/gap correcte → next-btn, incorrecte → feedback (tabindex=-1) ; goHome → premier exercise-card ; fb tabindex retiré à la réinitialisation. **Forme :** `data-per="SHS 21"` ajouté sur la carte index.html + badge `.badge-per` ajouté. Aucun CONFLIT À ARBITRER. Aucun point À VALIDER PAR ISAAC. |
| itineraires-decris.html | Décris le trajet (3H-4H) | ✅ | ✅ | **Pédagogie :** MSN 11 (Espace, C1) confirmé, cible 3H-4H, progression N1→N3 cohérente (distance et complexité croissantes, obstacles au N3 forçant le détour). Feedbacks formatifs (indication précise de l'étape en erreur et de ce qu'il faut corriger) conservés intacts. `TYPE_TACHE='instance'` déclaré et cohérent : le retry sur la même instance (corriger la séquence via Effacer/Recommencer puis Vérifier à nouveau, sans réinitialiser l'exercice) était déjà implémenté, maintenant formalisé. **Accessibilité :** légende "Ma séquence" et numéros d'étape 13px→18px avec couleur #94A3B8→#475569 (contraste insuffisant) ; message d'état vide de la séquence 15px→18px, même correctif couleur ; boutons Effacer/Recommencer/Vérifier : texte 15px→22px (seuil texte interactif) et cible tactile 48px→64px (boutons d'action, pas de simple navigation) ; espacement entre boutons adjacents (d-pad 6px→12px, rangée Effacer/Recommencer/Vérifier 10px→12px) ; communication couleur seule corrigée sur l'étape en erreur de la séquence (icône ⚠️ ajoutée + `aria-label` mentionnant désormais l'erreur, en plus du contour rouge) ; gestion du focus ajoutée (absente jusqu'ici) — `newBtn.focus()` sur réussite, `feedbackEl.focus()` sur erreur (tabindex="-1" statique ajouté aux deux), `questionBox.focus()` (tabindex="-1" ajouté) après chaque nouvel exercice/changement de niveau, à l'image de `balance.html`. **Forme :** icône ▶ ajoutée sur `new-btn` (recommandée hors 1H-2H, cohérence visuelle) ; migration app-base.css/appUtils.js déjà effectuée (aucune action requise). **Correctifs post-revue (`/code-review`, 2026-07-02, sur demande explicite d'Isaac) :** `showFeedback` branche désormais réellement sur `TYPE_TACHE` (`correct || TYPE_TACHE === 'drill'`, comportement inchangé mais conforme au contrat documenté) ; `renderSequence()` scrolle vers la puce en erreur (`scrollIntoView`) au lieu de toujours scroller vers la dernière puce, qui pouvait masquer une erreur détectée avant la fin de la séquence ; `verifySequence()` capture `state` dans `runState` et abandonne l'exécution si `state` a changé après un `await` (garde-fou), et les boutons de niveau sont désormais désactivés pendant l'animation (`updateButtons()` + `.level-btn:disabled` ajouté localement) — corrige une course : changer de niveau pendant l'animation de vérification pouvait faire lire/modifier par une exécution périmée l'état du nouvel exercice et lui voler le focus via les nouveaux appels `.focus()`. Vérifié en preview (Playwright) : succès, échec précoce (erreur à l'étape 1 sur une séquence de 5), verrouillage des boutons de niveau pendant l'animation, aucune erreur console. Les 2 constats PLAUSIBLE de la revue (pattern couleur-seule et gestion du focus non centralisés dans `appUtils.js`/`balance.html`) n'ont pas été appliqués : ils exigeraient de toucher un fichier partagé ou une autre app, hors périmètre absolu de cette routine — laissés à l'arbitrage d'Isaac. Aucun CONFLIT À ARBITRER. Aucun point À VALIDER PAR ISAAC. |
| itineraires.html | Lis le trajet (3H-4H) | ✅ | ✅ | **Pédagogie :** MSN 11 (Espace, C1) confirmé, cible 3H-4H, cohérent avec le sibling `itineraires-decris.html`. Tâche = retrouver la case d'arrivée parmi 3 candidats après lecture d'une séquence de déplacements. `TYPE_TACHE='instance'` déclaré (absent jusqu'ici) et cohérent avec le comportement déjà en place : les candidats ne sont jamais désactivés après une erreur, le feedback formatif indique quoi refaire (« relis chaque déplacement… » puis « recompte pas à pas… ») et l'élève retente sur la même instance — RAS, aucun changement de comportement, contrat seulement formalisé. Feedbacks formatifs et génération d'exercice conservés intacts. **Accessibilité :** gestion du focus ajoutée (absente jusqu'ici) — `newBtn.focus()` après réponse correcte (priorité 1, bouton d'action suivante visible), `tabindex="-1"` + `questionBox.focus()` en fin de `newExercise()` pour éviter la perte de focus quand « Nouvel exercice » se masque après clic (même précédent que `balance.html`/`itineraires-decris.html`) ; `aria-live="polite"` retiré de `#question-box` pour éviter un double-signalement lecteur d'écran maintenant que la mise à jour est portée par le focus explicite (alignement sur le précédent `balance.html`/`itineraires-decris.html`, qui n'ont jamais eu `aria-live` sur cet élément). **Tentative non retenue :** un plancher de largeur (`max(390px, …)`) avait été ajouté à `.grid-wrapper` pour garantir des cases candidates ≥64px en multitâche iPad étroit (Slide Over/Split View) ; la revue de code (`/code-review`, deux angles indépendants) a démontré empiriquement (Playwright à 350px) que ce plancher faisait déborder `.grid-wrapper` hors de `main` (centré par `align-items: center`), rendant une partie de la colonne 0 — potentiellement une case candidate réelle — inatteignable au scroll (scrollX non négatif en LTR) : pire que le défaut initial. **Retiré** ; `.grid-wrapper` restauré à sa largeur d'origine `min(420px, calc(100vw - 32px))`, revérifié à 350px (`docScrollWidth` = `windowWidth`, aucun débordement). **À VALIDER PAR ISAAC :** les cases candidates repassent sous 64px (≈51px mesurés à 350px de large) dans ce scénario de multitâche étroit — aucune solution robuste trouvée dans cette session (une grille à défilement local interne a été envisagée mais présente un risque de régression visuelle non entièrement vérifiable ici) ; à arbitrer/prioriser par Isaac. **Forme :** icône ▶ ajoutée sur `new-btn` (recommandée hors 1H-2H, cohérence visuelle) ; migration app-base.css/appUtils.js déjà effectuée (aucune action requise) ; carte index.html déjà conforme (`data-domain`, `data-harmos="c1"`, `data-per="MSN 11"`, badge `3H–4H`). **Correctifs post-revue (`/code-review --comment`, 2026-07-03) :** en plus du retrait du plancher de largeur ci-dessus, le chemin réponse incorrecte de `handleClick` dupliquait la logique de `showFeedback` (classe/texte du feedback, `playIncorrect()`) au lieu de l'appeler — consolidé en un appel `showFeedback(false, msg)`, comportement observable inchangé (même classe, même texte, même son), conforme à la règle design-portail « `showFeedback` point unique d'appel ». Vérifié en preview (Playwright) : résolution correcte après plusieurs essais erronés sur la même instance (message formatif variant au 2ᵉ essai, désormais via `showFeedback`), focus sur `new-btn` après succès puis sur `question-box` après « Nouvel exercice », aucun débordement horizontal à 350px, aucune erreur console. Aucun CONFLIT À ARBITRER. |
| longueurs-3h4h.html | Les longueurs | ✅ | ✅ | **Pédagogie :** PER MSN 14 (Grandeurs et mesures, longueur) confirmé, cible 3H-4H (badge de la carte index.html). Progression N1→N3 cohérente : N1 = 2 barres alignées (introduction) → N2 = 3 barres, orientations variées 0-65° (invariance de la longueur par rapport à l'orientation) → N3 = classement complet de 4 barres (transitivité de l'ordre). `TYPE_TACHE` absent → déclaré `{1:'drill', 2:'drill', 3:'instance'}` sur le modèle de `balance.html` : N1/N2 = choix parmi 2-3 barres, un retry serait un tâtonnement aveugle (cohérent avec `comparaison-nombres-*`/`capacites-*`) ; N3 = classement où l'erreur est partiellement exploitable (certaines barres déjà bien placées), cohérent avec l'exercice de tri de `ville-et-campagne-5h6h.html`. **Contrat de retry N3 manquant → ajouté :** `verifyL3` verrouillait l'exercice et révélait le rang correct (`→ 3`) dès la première erreur, empêchant toute correction — contraire au contrat `instance` de `skills/design-portail/SKILL.md`. Réécrit : le rang exact n'est plus révélé (donnerait la réponse), seules les barres mal placées sont nommées dans le feedback ; `verify-btn` reste actif, `new-btn` cache jusqu'à réussite ; l'élève retire/replace les barres citées (bouton ou tap) et revérifie sur le même classement. Vérifié en preview (Playwright) : 21 tentatives erronées consécutives sur un classement de 4 barres, feedback et `verify-btn` corrects à chaque tour, réussite détectée au 22ᵉ essai. Feedback formatif conservé intact (le canvas et les boutons marquent visuellement ✓/✗ après chaque réponse, ce qui sert d'explication visuelle du « pourquoi »). **Accessibilité :** aucun moyen clavier de répondre n'existait (le canvas seul gère le tap/clic, aucune alternative — WCAG 2.1.1) alors que le CSS `.choice-btn`/`#choices` prévu à cet effet existait déjà mais n'était jamais rempli en JS ; câblé : un bouton réel par barre (accessible clavier nativement), synchronisé à chaque `draw()`, focus restauré sur le même bouton après reconstruction du DOM. `.choice-btn` : taille de texte 21px→22px (seuil texte interactif) ; `:focus-visible` ajouté (absent). `.hint` : contraste `#94A3B8`→`#475569` (~2,3:1 insuffisant) et taille 14px→18px (seuil texte non interactif). `drawL2` ne marquait que la barre correcte (✓) après réponse, sans distinguer visuellement la barre choisie à tort (communication par la couleur seule sur le choix erroné) — `drawL1` le faisait déjà correctement (✓/✗) : croix ✗ ajoutée sur la barre choisie à tort en N2, par cohérence. Gestion du focus ajoutée (absente jusqu'ici) : `newBtn.focus()` après réponse N1/N2 et après succès N3 (bouton d'action suivante visible) ; `feedbackEl.focus()` (tabindex=-1) après échec N3 (aucun bouton d'action suivante, contrat `instance`+incorrect) ; `questionBox.focus()` (tabindex=-1 statique) en fin de `newExercise()`. Canvas marqué `aria-hidden="true"` (l'information est désormais portée par les boutons accessibles, le canvas devient un support visuel dupliqué). **Forme :** styles inline `.app-header`, `.back-link`, `.back-link:hover`, `.app-header h1`, `.header-spacer`, `main` retirés (dupliquaient app-base.css, CLAUDE.md interdiction #2 — seule reste la surcharge `--app-header-bg: #C0272D`, couleur de branche Mathématiques) ; icône `▶` ajoutée sur `new-btn` (recommandée hors 1H-2H). **Décision Isaac :** fichier renommé `longueurs.html` → `longueurs-3h4h.html` (convention de suffixe de groupe d'années, CLAUDE.md) ; `href` mis à jour dans `index.html`, référence corrigée dans `skills/pedagogie-per/SKILL.md`. Aucun CONFLIT À ARBITRER. |
| saut-de-nombre-3h4h.html | Le saut de nombre (3H-4H) | ✅ | ✅ | **Scission décidée par Isaac (2026-07-05)** de l'ancien `saut-de-nombre.html`, qui couvrait à tort trois groupes d'années dans un seul fichier (N1 10–99, N2 100–999, N3 1000–9999) sous une carte unique `5H–6H` — violation de la convention « une app = un groupe d'années » (`CLAUDE.md`), déjà rencontrée avec `capacites-1h2h/3h4h` et `comparaison-nombres-3h4h/5h6h/7h8h`. Cible **3H-4H**, MSN 12 (Nombres, C1). N1 · jusqu'à 50 (2-3 sauts) → N2 · jusqu'à 99 (3-5 sauts), même jeu de boutons (±1/±10) aux deux niveaux — seule la plage progresse, sur le modèle « 1re partie / 2e partie du cycle » (`skills/pedagogie-per/SKILL.md`). Contextes situés, feedback formatif et logique `showFeedback`/`TYPE_TACHE='instance'` repris tels qu'audités sur l'ancien fichier (voir audit v2 ci-dessous, non dupliqué ici). Vérifié en preview (Playwright) : parcours complet aux 2 niveaux, aucune erreur console, rendu visuel conforme. Aucun CONFLIT À ARBITRER. |
| saut-de-nombre-5h6h.html | Le saut de nombre (5H-6H) | ✅ | ✅ | **Scission décidée par Isaac (2026-07-05)**, voir `saut-de-nombre-3h4h.html` pour le contexte. Cible **5H-6H**, MSN 22 (Nombres, C2). N1 · jusqu'à 500 (2-3 sauts) → N2 · jusqu'à 999 (3-5 sauts), boutons ±1/±10/±100 aux deux niveaux. Aucun CONFLIT À ARBITRER. |
| saut-de-nombre-7h8h.html | Le saut de nombre (7H-8H) | ✅ | ✅ | **Anciennement `saut-de-nombre.html`** (renommé) — scindé sur décision d'Isaac (2026-07-05) : l'app couvrait en réalité trois groupes d'années dans un seul fichier (N1 10–99 ≈ 3H-4H, N2 100–999 ≈ 5H-6H, N3 1000–9999 ≈ 7H-8H) sous une seule carte `5H–6H`/`MSN 22`, en violation de la convention « une app = un groupe d'années » (`CLAUDE.md`) déjà rencontrée avec `capacites-1h2h/3h4h` et `comparaison-nombres-3h4h/5h6h/7h8h` ; signalé en `CONFLIT À ARBITRER` dans l'audit v2 initial (PR #32), résolu par la scission en 3 apps (`saut-de-nombre-3h4h.html`, `saut-de-nombre-5h6h.html`, ce fichier). Cible **7H-8H**, MSN 22 (Nombres, C2), plage retenue jusqu'à 9999 (reprend le plafond de l'ancien N3). N1 · jusqu'à 5000 (2-3 sauts) → N2 · jusqu'à 9999 (3-5 sauts), boutons ±1/±10/±100/±1000 aux deux niveaux. **Audit v2 (avant scission, PR #32) — Pédagogie :** MSN 22 confirmé comme famille d'objectif ; ancrage dans des contextes concrets (kangourou, train, grenouille, fusée, robot) conforme au principe didactique « toujours un contexte » ; feedback formatif déjà explicatif (indique le sens et l'ordre de grandeur du saut, pas seulement juste/faux) — conservé intact dans les 3 fichiers issus de la scission. `TYPE_TACHE='instance'` déclaré et cohérent : une réponse incorrecte n'invalide rien, l'élève ajuste sa trajectoire de sauts et revérifie sur le même nombre cible jusqu'à réussite. **Accessibilité :** boutons de saut déjà de vrais `<button>` (accessibles au clavier nativement, aucun canvas) — gestion du focus ajoutée : `newBtn.focus()` après réussite, focus sur `feedbackEl` (`tabindex="-1"`) après une réponse incorrecte, `questionBox.focus()` (`tabindex="-1"` ajouté) en fin de `newExercise()`. `.number-label` 14px/`#94A3B8` → 18px/`#475569` ; `.target-label` 15px→18px (contraste déjà conforme) ; `.jump-btn` porté à 22px minimum (seuil texte interactif) ; `:focus-visible` ajouté. **Forme :** styles inline dupliquant `app-base.css` retirés (`.app-header`, `.back-link`, `.app-header h1`, `.header-spacer`, `main`) ; `--color-units/tens/hundreds` et `colorForRank`/`PLACE_COLORS` redéfinis inline en doublon d'`app-base.css`/`appUtils.js` — retirés (CLAUDE.md #2) ; icône `▶` ajoutée sur `new-btn`. **Revue de code (`/code-review --comment`, 8 angles, sur l'ancien fichier avant scission) :** corrigé directement (qualité pure) — override partiel de `main` retiré, `document.querySelectorAll('.jump-btn')` mis en cache dans `state.jumpBtns`, champ `grid` redondant retiré. Repris à l'identique dans les 3 fichiers de la scission, où la duplication `configs`/`btnDefs` par niveau signalée en commentaire PR ne se pose d'ailleurs plus : chaque fichier n'ayant plus qu'une seule échelle de sauts, `BTN_DEFS` est désormais unique par fichier (plus de table par niveau). Vérifié en preview (Playwright) sur les 3 fichiers post-scission : parcours complet aux 2 niveaux de chacun, aucune erreur console, rendu visuel conforme. Aucun CONFLIT À ARBITRER restant. |
| soustraction-colonne.html | Soustraction en colonne | ✅ | ✅ | **Pédagogie :** MSN 23 (Opérations, C2) confirmé, cible 5H-6H (badge de la carte), plages numériques ≤999 conformes au PER 5H-6H (nombres jusqu'à 1000). Progression N1→N4 cohérente : N1 (2 chiffres, sans emprunt) → N2 (3 chiffres, sans emprunt) → N3 (2 chiffres, 1 emprunt) → N4 (3 chiffres, ≥2 emprunts). `TYPE_TACHE` absent → déclaré `'instance'` (le feedback nomme les colonnes fausses, l'élève corrige et revérifie sur la même soustraction). Feedbacks formatifs conservés intacts. Aucune consigne n'était affichée à l'écran (`.question-box` absent, déviation de la structure canonique de `_template.html`) : ajoutée, contenu variable selon le niveau (mention du geste d'emprunt uniquement aux niveaux 3-4). **Accessibilité :** piège clavier corrigé — aux niveaux 1-2 (sans emprunt possible), les cellules du multiplicande portaient `role="button" tabindex="0"` sans aucun gestionnaire attaché ; restreint aux colonnes réellement `tappable` (niveaux 3-4). `aria-pressed` (dynamique) + `aria-label` enrichi (valeur effective annoncée) ajoutés sur les cellules d'emprunt (absents jusqu'ici). Gestion du focus ajoutée (absente jusqu'ici) : focus sur `new-btn` après réussite, sur `feedback` (`tabindex="-1"`) après échec (`skills/accessibilite/SKILL.md` §6). `verify-btn` désactivé tant que les cases de résultat ne sont pas toutes remplies (item impératif de la checklist design-portail, absent jusqu'ici). Communication par la couleur seule corrigée : un ✓/✗ non coloré superposé sur chaque case de résultat en plus de la bordure colorée existante, `aria-invalid` ajouté sur chaque `input`. Tailles de texte non interactif portées à 18px minimum : `.th-col` (en-têtes C/D/U, 13→18px), `.plus10-label` (13→18px, contraste ~3,3:1→~10:1 puis couleur finale `#475569`, voir revue ci-dessous), `.tap-hint` (10→18px, `#CBD5E0`→`#475569`). Cible tactile `.result-inp` portée à 64×64px minimum à tous les breakpoints (62×70px en base et 52×60px sous 400px → 64×70/64×64). `aria-atomic="true"` ajouté sur `#feedback`. **Forme :** icône ▶ ajoutée sur `new-btn` (recommandée hors 1H-2H) ; le "✓" décoratif de `verify-btn` passé en `aria-hidden="true"`. Migration app-base.css/appUtils.js déjà effectuée (aucune action requise). **À VALIDER PAR ISAAC :** le fichier s'appelle `soustraction-colonne.html`, sans le suffixe de groupe d'années (`-5h6h`) exigé par la convention de nommage de `CLAUDE.md` pour toute app ciblant un groupe précis (la carte porte `data-harmos="c2"` + badge `5H–6H`) ; non renommé dans cette routine car un renommage touche le `href` dans `index.html` et d'éventuelles références croisées — décision toujours prise explicitement par Isaac dans les audits précédents (`longueurs.html`, `comparaison-nombres.html`, `capacites.html`). Aucun CONFLIT À ARBITRER. Vérifié en preview (Playwright) : parcours complet niveaux 1 et 3 (mauvaise réponse → focus feedback, correction → focus new-btn, « Nouvel exercice » → check désactivé de nouveau), aucune fausse aria sur les cellules non empruntables au niveau 1, aucune erreur console. **Revue de code (`/code-review --comment`, 8 angles, PR #33) :** corrigé directement (qualité pure) — `canBorrowCol(c)` extrait (était recalculé différemment dans `buildTable()`/`updateMinuendDisplay()`), double appel `effectiveDigit()` évité, `aria-label` des cellules d'emprunt dédupliqué, `resetButtons()` et `showFeedback(correct, msg)` extraits (bloc de réinitialisation dupliqué 3×, logique de feedback désormais au patron canonique `skills/design-portail/SKILL.md`), `checkBtn.disabled = true` ajouté quand masqué après succès. 3 constats touchant un seuil d'accessibilité ou un choix de sens pédagogique laissés en commentaire PR pour arbitrage, puis **corrigés sur demande explicite d'Isaac** : `.result-inp:focus` réordonné après `.ok`/`.bad` (même spécificité CSS, l'ordre de déclaration décidait — l'anneau de focus était invisible sur un input marqué faux que l'élève rouvre pour le corriger) ; glyphe ✓/✗ de `.td-result` porté 16→18px (seuil texte non interactif) ; couleur du libellé « +10 » sortie de `var(--app-correct-fg)` (réutilisait le vert « réponse correcte », risque de signal mélangé pour un jeune élève) vers `#475569`, neutre et distinct de la palette valeur-de-position comme du vert de succès. Revérifié en preview (Playwright) après chaque vague de correctifs : aucune régression, aucune erreur console. Aucun CONFLIT À ARBITRER restant. |
| surfaces-5h6h.html | Les surfaces (5H-6H) | ✅ | ✅ | **Pédagogie :** MSN 24 (Grandeurs et mesures, aire, C2) confirmé, plages et progression N1 (comparaison sur grille, comptage de carreaux) → N2 (calcul l × L) cohérentes avec les attentes fondamentales PER (« calcule des aires de figures simples »). `TYPE_TACHE` absent → déclaré `'drill'`, cohérent avec le comportement déjà implémenté (choix parmi 3 surfaces, un retry reviendrait à deviner entre les 2 restantes). Feedbacks formatifs (expliquent le comptage ou le calcul, pas seulement juste/faux) conservés intacts. **Accessibilité :** texte à l'intérieur des boutons de choix (`.shape-label`, `.dim-label`) 15px/17px → 22px (seuil texte interactif) ; `.grid-note` 14px/`#94A3B8` (contraste ≈2,6:1) → 18px/`#475569` (≈7,6:1) ; communication par la couleur seule corrigée sur les surfaces correcte/incorrecte — ✓/✗ ajoutés au libellé + `aria-label` enrichi (« — bonne/mauvaise réponse ») en plus des bordures colorées existantes ; `role="banner"` ajouté sur `<header>` (absent) ; `:focus-visible` ajouté sur `.shape-item` (absent, aucun indicateur de focus clavier sur les boutons de choix) ; transition `border-color`/`background` de `.shape-item` déplacée sous `@media (prefers-reduced-motion: no-preference)` ; gestion du focus ajoutée (absente jusqu'ici) : `newBtn.focus()` après validation, focus sur le premier `.shape-item` après « Nouvel exercice » et changement de niveau (nouveau `newExercise()` enveloppant `buildExercise()`, l'appel initial au chargement reste sans déplacement de focus). **Forme :** couleurs dupliquant des variables déjà fournies par `app-base.css` remplacées par `var(--app-border)`, `var(--app-correct-bg)`, `var(--app-wrong-bg)` (CLAUDE.md #2) ; icône ▶ ajoutée sur `new-btn` (recommandée hors 1H-2H, cohérence visuelle). Migration app-base.css/appUtils.js déjà effectuée. Vérifié en preview (Playwright) : parcours complet aux 2 niveaux, focus déplacé correctement après validation/nouvel exercice/changement de niveau, icônes ✓/✗ affichées, aucune erreur console. Aucun CONFLIT À ARBITRER. Aucun point À VALIDER PAR ISAAC. |
| surfaces-3h4h.html | Les surfaces (3H-4H) | ✅ | ❌ | |
| valeur-position.html | L'extracteur | ✅ | ❌ | |
| vertical-calculation.html | Additions en colonne | ✅ | ❌ | |
| sujet-predicat-5h6h.html | Sujet et prédicat | ✅ | ❌ | |
| complement-dizaine-3h4h.html | Le complément à 10 (3H-4H) | ✅ | ❌ | |
| bonne-operation-3h4h.html | La bonne opération (3H-4H) | ✅ | ❌ | |
| coordonnees-5h6h.html | Les coordonnées | ✅ | ❌ | |
| doubles-moities-3h4h.html | Doubles et moitiés | ✅ | ❌ | |
| denombrement-1h2h.html | Le dénombrement (1H-2H) | ✅ | ❌ | |
| complement-phrase-5h6h.html | Le complément de phrase | ✅ | ❌ | Nouvelle app (5H-6H, L1 26). Créée depuis `_template.html` (app-base.css + appUtils.js + feedbackUtils.js). `TYPE_TACHE='instance'` (identifier/manipuler le CP = tâche corrigeable). N1 repérer le CP · N2 enlever pour vérifier · N3 déplacer. Variables `--color-sujet/predicat/cp` propres à l'app (distinctes de la palette valeur-de-position). **Note d'arbitrage (accessibilité > design) :** le déplacement N3 est exposé au tap et au clavier (bouton réel), le glisser n'étant qu'un enrichissement — pas de drag-and-drop exclusif (skill accessibilité §1). Pas encore audité v2. |
| addition-1h2h.html | Mes additions (1H-2H) | ✅ | ❌ | |
| ou-est-il-1h2h.html | Où est-il ? (1H-2H) | ✅ | ❌ | |
| bonne-operation-7h8h.html | La bonne opération (7H-8H) | ✅ | ❌ | |
| axes-symetrie-7h8h.html | Les axes de symétrie (7H-8H) | ✅ | ❌ | |
| conversions-mesures-7h8h.html | Les conversions de mesures (7H-8H) | ✅ | ❌ | |
| tri-animaux-3h4h.html | Trie les animaux (3H-4H) | ✅ | ❌ | |
| dizaines-unites-3h4h.html | Dizaines et unités | ✅ | ❌ | |
| champ-lexical-5h6h.html | Le champ lexical | ✅ | ❌ | |
